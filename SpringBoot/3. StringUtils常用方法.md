## ==3.==StringUtils常用方法

> <kbd>2025.03.11</kbd> <kbd>公众号</kbd> <kbd>腾讯元宝</kbd>
>
> ==**org.apache.commons.lang3.StringUtils;**==
>
> >==isEmpty系列==
> >
> >`StringUtils.isEmpty()`：null或空字符串才是true
> >
> >```java
> >StringUtils.isEmpty(null) = true
> >StringUtils.isEmpty("") = true
> >StringUtils.isEmpty(" ") = false
> >StringUtils.isEmpty("ikun") = false
> >StringUtils.isEmpty(" ikun ") = false
> >```
> >
> >`StringUtils.isNotEmpty()`：相当于不为空 ,=` !isEmpty()`
> >
> >`StringUtils.isAnyEmpty()`：有空就返回`true`
> >
> >```java
> >StringUtils.isAnyEmpty(null)             = true
> >StringUtils.isAnyEmpty(null, "foo")      = true
> >StringUtils.isAnyEmpty("", "bar")        = true
> >StringUtils.isAnyEmpty("bob", "")        = true
> >StringUtils.isAnyEmpty("  bob  ", null)  = true
> >StringUtils.isAnyEmpty(" ", "bar")       = false
> >StringUtils.isAnyEmpty("foo", "bar")     = false
> >StringUtils.isAnyEmpty(new String[]{})   = false
> >StringUtils.isAnyEmpty(new String[]{""}) = true
> >```
> >
> >`StringUtils.isNoneEmpty()`：相当于`!isAnyEmpty(css)` , 必须所有的值都不为空才返回true
> >
> >```java
> >StringUtils.isNoneEmpty(null)             = false
> >StringUtils.isNoneEmpty(null, "foo")      = false
> >StringUtils.isNoneEmpty("", "bar")        = false
> >StringUtils.isNoneEmpty("bob", "")        = false
> >StringUtils.isNoneEmpty("  bob  ", null)  = false
> >StringUtils.isNoneEmpty(new String[] {})  = true
> >StringUtils.isNoneEmpty(new String[]{""}) = false
> >StringUtils.isNoneEmpty(" ", "bar")       = true
> >StringUtils.isNoneEmpty("foo", "bar")     = true
> >```
> >
> >==isBank系列==
> >
> >`StringUtils.isBlank()`：是否为真空值(空格或者空值)
> >
> >```java
> >StringUtils.isBlank(null)      = true
> >StringUtils.isBlank("")        = true
> >StringUtils.isBlank(" ")       = true
> >StringUtils.isBlank("bob")     = false
> >StringUtils.isBlank("  bob  ") = false
> >```
> >
> >`StringUtils.isNotBlank()`：是否真的不为空,不是空格或者空值 ,相当于`!isBlank();`
> >
> >`StringUtils.isAnyBlank()`：是否包含任何真空值(包含空格或空值)
> >
> >```java
> >StringUtils.isAnyBlank(null)             = true
> >StringUtils.isAnyBlank(null, "foo")      = true
> >StringUtils.isAnyBlank(null, null)       = true
> >StringUtils.isAnyBlank("", "bar")        = true
> >StringUtils.isAnyBlank("bob", "")        = true
> >StringUtils.isAnyBlank("  bob  ", null)  = true
> >StringUtils.isAnyBlank(" ", "bar")       = true
> >StringUtils.isAnyBlank(new String[] {})  = false
> >StringUtils.isAnyBlank(new String[]{""}) = true
> >StringUtils.isAnyBlank("foo", "bar")     = false
> >```
> >
> >`StringUtils.isNoneBlank()`：是否全部都不包含空值或空格
> >
> >```java
> >StringUtils.isNoneBlank(null)             = false
> >StringUtils.isNoneBlank(null, "foo")      = false
> >StringUtils.isNoneBlank(null, null)       = false
> >StringUtils.isNoneBlank("", "bar")        = false
> >StringUtils.isNoneBlank("bob", "")        = false
> >StringUtils.isNoneBlank("  bob  ", null)  = false
> >StringUtils.isNoneBlank(" ", "bar")       = false
> >StringUtils.isNoneBlank(new String[] {})  = true
> >StringUtils.isNoneBlank(new String[]{""}) = false
> >StringUtils.isNoneBlank("foo", "bar")     = true
> >```
> >
> >| 方法名                          | 中文解释                                           |
> >| ------------------------------- | -------------------------------------------------- |
> >| **判空检查**                    |                                                    |
> >| IsEmpty/IsBlank                 | 检查字符串是否为空或仅包含空白字符                 |
> >| **空白处理**                    |                                                    |
> >| Trim/Strip                      | 删除字符串首尾的空白字符                           |
> >| **安全比较**                    |                                                    |
> >| Equals/Compare                  | 支持null值的字符串比较                             |
> >| **首尾检查**                    |                                                    |
> >| startsWith                      | 安全检查字符串是否以指定前缀开头                   |
> >| endsWith                        | 安全检查字符串是否以指定后缀结尾                   |
> >| **索引查询**                    |                                                    |
> >| IndexOf/LastIndexOf/Contains    | 安全查找子字符串位置/判断包含关系                  |
> >| IndexOfAny/LastIndexOfAny       | 查找任意指定字符首次/最后一次出现的位置            |
> >| IndexOfAnyBut/LastIndexOfAnyBut | 查找非指定字符首次/最后一次出现的位置              |
> >| **字符集检查**                  |                                                    |
> >| ContainsOnly                    | 检查字符串是否仅包含指定字符集中的字符             |
> >| ContainsNone                    | 检查字符串是否不包含指定字符集中的任何字符         |
> >| ContainsAny                     | 检查字符串是否包含指定字符集中的任意字符           |
> >| **子串操作**                    |                                                    |
> >| Substring/Left/Right/Mid        | 安全截取子字符串（从左/右/中间截取）               |
> >| SubstringBefore/SubstringAfter  | 截取指定分隔符前/后的子字符串                      |
> >| SubstringBetween                | 截取两个分隔符之间的子字符串                       |
> >| **拆分合并**                    |                                                    |
> >| Split/Join                      | 将字符串拆分为数组/将数组合并为字符串              |
> >| **删除替换**                    |                                                    |
> >| Remove/Delete                   | 删除字符串的指定部分                               |
> >| Replace/Overlay                 | 替换字符串中的指定内容                             |
> >| **首尾处理**                    |                                                    |
> >| Chomp/Chop                      | 删除字符串结尾的换行符/删除最后一个字符            |
> >| AppendIfMissing                 | 如果字符串不以指定后缀结尾，则追加后缀             |
> >| PrependIfMissing                | 如果字符串不以指定前缀开头，则添加前缀             |
> >| **填充处理**                    |                                                    |
> >| LeftPad/RightPad/Center/Repeat  | 左填充/右填充/居中填充/重复字符串                  |
> >| **大小写转换**                  |                                                    |
> >| UpperCase/LowerCase             | 转换为全大写/全小写                                |
> >| SwapCase                        | 交换大小写                                         |
> >| Capitalize/Uncapitalize         | 首字母大写/首字母小写                              |
> >| **计数校验**                    |                                                    |
> >| CountMatches                    | 统计子字符串出现的次数                             |
> >| IsAlpha/IsNumeric               | 检查字符串是否全为字母/数字                        |
> >| IsWhitespace                    | 检查字符串是否全为空白字符                         |
> >| IsAsciiPrintable                | 检查字符串是否全部为ASCII可打印字符                |
> >| **默认处理**                    |                                                    |
> >| DefaultString                   | 将null转换为空字符串                               |
> >| **高级操作**                    |                                                    |
> >| Rotate                          | 循环位移字符串                                     |
> >| Reverse/ReverseDelimited        | 反转字符串/按分隔符反转                            |
> >| Abbreviate                      | 用省略号缩写字符串                                 |
> >| Difference                      | 比较两个字符串的差异                               |
> >| LevenshteinDistance             | 计算两个字符串的编辑距离（转换所需的最少操作次数） |
> >
> >⚠️ 使用注意：
> >
> >1. 部分方法存在版本差异（如`Strip`是较新版本的方法）
> >2. 实际方法名严格遵循驼峰命名法（如`prependIfMissing`应为`PrependIfMissing`）
> >3. 建议结合官方文档使用
> >
> >---
>
> **==org.springframework.util.StringUtils==**
>
> >| 方法名                                       | 参数说明                                 | 中文解释                                                   |
> >| -------------------------------------------- | ---------------------------------------- | ---------------------------------------------------------- |
> >| **判空处理**                                 |                                          |                                                            |
> >| `hasLength(String str)`                      | `str`: 输入字符串                        | 检查字符串是否非空（长度 > 0）                             |
> >| `hasText(String str)`                        | `str`: 输入字符串                        | 检查字符串是否包含非空白字符（非空且至少一个非空格）       |
> >| `isEmpty(Object str)`                        | `str`: 输入对象                          | 检查字符串是否为 `null` 或空字符串（兼容非字符串对象）     |
> >| **字符串处理**                               |                                          |                                                            |
> >| `trimWhitespace(String str)`                 | `str`: 输入字符串                        | 删除字符串首尾的空白字符                                   |
> >| `trimAllWhitespace(String str)`              | `str`: 输入字符串                        | 删除字符串中所有空白字符                                   |
> >| `quote(String str)`                          | `str`: 输入字符串                        | 为字符串添加双引号（若为 `null` 返回 `null`）              |
> >| `unquote(String str)`                        | `str`: 输入字符串                        | 移除字符串首尾的双引号                                     |
> >| **分隔符处理**                               |                                          |                                                            |
> >| `commaDelimitedListToStringArray`            | `str`: 逗号分隔的字符串                  | 将逗号分隔的字符串转换为数组                               |
> >| `collectionToDelimitedString`                | `coll`: 集合, `delim`: 分隔符            | 将集合元素用分隔符连接成字符串                             |
> >| `arrayToDelimitedString`                     | `arr`: 数组, `delim`: 分隔符             | 将数组元素用分隔符连接成字符串                             |
> >| `splitArrayElementsIntoProperties`           | `arr`: 数组, `delim`: 分隔符             | 将数组元素按分隔符拆分为键值对（返回 `Properties` 对象）   |
> >| **路径处理**                                 |                                          |                                                            |
> >| `cleanPath(String path)`                     | `path`: 文件路径                         | 规范化文件路径（去除 `../` 和 `./` 等冗余部分）            |
> >| `getFilename(String path)`                   | `path`: 文件路径                         | 提取路径中的文件名部分（如 `/test/file.txt` → `file.txt`） |
> >| `stripFilenameExtension(String path)`        | `path`: 文件路径                         | 去除文件名扩展名（如 `file.txt` → `file`）                 |
> >| **特殊操作**                                 |                                          |                                                            |
> >| `replace(String in, String old, String new)` | `in`: 原字符串, `old`: 旧值, `new`: 新值 | 替换字符串中的指定子串（支持 `null` 值处理）               |
> >| `delimitedListToStringArray`                 | `str`: 输入字符串, `delim`: 分隔符       | 按自定义分隔符将字符串拆分为数组                           |
> >| `parseLocaleString(String locale)`           | `locale`: 区域字符串（如 `en_US`）       | 将字符串解析为 `Locale` 对象                               |
> >
> >Spring 的 `StringUtils` 更侧重基础操作和路径处理，而 Apache 的版本功能更全面（如字符串截取、填充、Levenshtein距离等）。
> >
> >所有方法均支持 `null` 输入，并返回合理值（如 `hasText(null)` → `false`）。
> >
> >部分方法在 Spring 5.x 后新增（如 `trimAllWhitespace`）。


